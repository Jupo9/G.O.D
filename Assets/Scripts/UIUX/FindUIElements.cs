using UnityEngine;

public class FindUIElements : MonoBehaviour
{
    /// <summary>
    /// IDK, tbh
    /// </summary>
    /// 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
