using UnityEngine;

//RequireComponent adds other type like a script to the component list, if this scipt is add as component
[RequireComponent (typeof(AngelUI))]
public class AngelManager : MonoBehaviour
{
    /// <summary>
    /// THIS SCRIPT CONTAINS:
    /// Add AngelUI Script as Component
    /// </summary>
}
